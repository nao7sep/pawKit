{
  "_comment": "Default log level is Debug for testing. Microsoft libraries are set to Warning to reduce noise.",
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft": "Warning"
    }
  }
}
